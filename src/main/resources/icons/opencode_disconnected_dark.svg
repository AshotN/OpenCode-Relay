<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 16 16" fill="none">
  <!-- Bezel (dimmed, 1px border) -->
  <rect x="2" y="1" width="12" height="14" fill="#CED0D6" opacity="0.4"/>
  <!-- Recessed screen area (dimmed, inset 1px) -->
  <rect x="3" y="2" width="10" height="12" fill="#1E1F22" opacity="0.4"/>
  <!-- Title bar strip (dimmed) -->
  <rect x="3" y="2" width="10" height="2" fill="#43454A" opacity="0.4"/>
  <!-- Prompt: '>' chevron (dimmed) -->
  <path d="M5 10L6 11L5 12" stroke="#CED0D6" stroke-width="1" stroke-linecap="square" stroke-linejoin="miter" opacity="0.4"/>
  <!-- Cursor: '_' underscore with gap (dimmed) -->
  <line x1="8" y1="12" x2="11" y2="12" stroke="#CED0D6" stroke-width="1" stroke-linecap="square" opacity="0.4"/>
  <!-- Disconnected modifier: red dot, bottom-right -->
  <circle cx="13" cy="13" r="3" fill="#C75450"/>
</svg>
